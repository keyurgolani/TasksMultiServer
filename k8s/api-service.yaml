apiVersion: v1
kind: Service
metadata:
  name: rest-api
  namespace: task-manager
spec:
  selector:
    app: rest-api
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  type: LoadBalancer
