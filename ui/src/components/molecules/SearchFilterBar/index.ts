/**
 * SearchFilterBar Molecule
 *
 * A composite molecule that combines SortFilterButton and SearchBar
 * into a unified search and filter interface.
 */

export { SearchFilterBar, type SearchFilterBarProps } from "./SearchFilterBar";
export { default } from "./SearchFilterBar";
